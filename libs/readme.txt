---because arduino library manager sucks, i've added here the used libraries that i've downloaded separately

Steps to add a new zip library:
	Sketch > Include Library > Add .ZIP library

Afterwards, the library can be included using Sketch > Include library :)